plone.app.page
--------------

 [x] Create simplified add form for pages
 [x] Register ``sitelayout`` and ``pagelayout`` traversers

 [x] Improve management of site layout
    - Create a view @@page-site-layout
        - available on all ILayout
        - calculates appropriate site layout using getPageSiteLayout()
        - caches per context
    - Create a view @@default-site-layout
        - available on *
        - calculates appropriate site layout using getDefaultSiteLayout()
        - caches per context

 [ ] Invalidate cache key in sitelayout.py if global default site layout 
     registry record is changed. Can use RecordModifiedEvent, but need a
     counter or similar.

 [ ] Define a registry structure for site layouts and page layout templates
    - Title
    - Description
    - Optional thumbnail?
 [ ] Define a manifest format for layout metadata
 [ ] Define an import mechanism for this manifest
    [ ] Filesystem resource directories
    [ ] Resource directories in distributions
    [ ] Resources created TTW

 [ ] Add vocabulary of available page layout templates
 [ ] Add vocabulary of available site layouts
 [ ] Apply to ILayout

 [ ] Create helper to create new template page layouts from a content item
 [ ] Create helpers to change category of a content item
    - This means changing the portal_type to a different PageFTI instance

 [ ] Create per-item behaviour adapter

 [ ] Tests for
    [ ] getDefaultPageLayout()
    [ ] getDefaultSiteLayout()
    [ ] getPageSiteLayout()
    [ ] getPageTypes()
    [ ] clonePageType()
    [ ] getDefaultPageContent()
    [ ] changePageType()
    [ ] Resource traversers
    [ ] Layout indirection views
    [ ] Layout indirection view cache key

plone.app.blocks
----------------

 [ ] The DOCTYPE is lost after applying the XSLT in tile page rendering mode
 [ ] Create pure XSLT version

plone.app.tiles
---------------

 [ ] Create "null add form" view for tiles with no configuration
 [ ] Fix test failures
 [ ] Fix test errors when run in aggregate with other tests
 
plone.app.standardtiles
-----------------------

 [ ] Split tests up - it's impossible to debug one giant doctest
 [ ] Fix test failures under Plone 4.1

plone.app.deco
--------------

 [ ] Define "can edit layout" permission
 [ ] Fix test failure when run in aggregate with other tests

plone.app.controlpanel
----------------------

 [ ] Fix test failures

plone.app.content
-----------------

 [ ] Fix test failures

plone.app.workflow
------------------

 [ ] Fix test failures
 

